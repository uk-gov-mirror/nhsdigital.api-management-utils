steps:
  - bash: |
      sudo apt-get install git -y
      git clone https://github.com/NHSDigital/api-management-utils.git utils
      cd utils
      git checkout apm-1191-common-pipelines
      pip install --upgrade pip setuptools
      pip install poetry
      poetry install
    displayName: Set up utils
